"""Planning engine: forecasting, inventory policy, projection, exceptions and backtest.

This package contains only business logic. It has no dependency on Streamlit or Plotly,
so every calculation can be imported, unit-tested and cross-checked against the Excel
validation workbook independently of the user interface.
"""
